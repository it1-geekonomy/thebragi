import { AccountLayout } from "@/shared/layouts/AccountLayout";

export default function AccountRouteGroupLayout({ children }: { children: React.ReactNode }) {
  return <AccountLayout>{children}</AccountLayout>;
}
