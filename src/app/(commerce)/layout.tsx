import { CommerceLayout } from "@/shared/layouts/CommerceLayout";

export default function CommerceRouteGroupLayout({ children }: { children: React.ReactNode }) {
  return <CommerceLayout>{children}</CommerceLayout>;
}
